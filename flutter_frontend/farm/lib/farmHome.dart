import 'package:flutter/material.dart';

class Farmhome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('Smart Farm')));
  }
}
